<UserControl x:Class="FoxTunes.Volume"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Core="clr-namespace:FoxTunes;assembly=FoxTunes.Core"
             xmlns:Windows="clr-namespace:FoxTunes" 
             xmlns:ViewModel="clr-namespace:FoxTunes.ViewModel"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Core:Core, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ViewModel:Volume x:Key="ViewModel" Core="{Binding}"></ViewModel:Volume>
    </UserControl.Resources>
    <Grid>
        <Slider VerticalAlignment="Center" IsMoveToPointEnabled="True" Value="{Binding Value, Source={StaticResource ViewModel}}" Minimum="0" Maximum="1" SmallChange="0.01" LargeChange="0.1"></Slider>
    </Grid>
</UserControl>
