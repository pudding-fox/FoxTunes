<Windows:UIComponentBase x:Class="FoxTunes.Spectrum"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Core="clr-namespace:FoxTunes;assembly=FoxTunes.Core" 
             xmlns:Windows="clr-namespace:FoxTunes" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance {x:Type Core:Core}, IsDesignTimeCreatable=False}" 
             Background="Transparent" 
             Foreground="{DynamicResource TextBrush}"
             SnapsToDevicePixels="True">
    <UserControl.ContextMenu>
        <Windows:Menu Category="{Binding Source={x:Static Windows:SpectrumBehaviour.CATEGORY}}"></Windows:Menu>
    </UserControl.ContextMenu>
    <Border Name="Border" BorderBrush="{DynamicResource NormalBorderBrush}" BorderThickness="1" Padding="1"></Border>
</Windows:UIComponentBase>
