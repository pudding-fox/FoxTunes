<UserControl x:Class="FoxTunes.PlaybackDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Core="clr-namespace:FoxTunes;assembly=FoxTunes.Core" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Core:Core, IsDesignTimeCreatable=False}">
    <StackPanel>
        <TextBlock>
            <Run Text="{x:Static Core:Publication.Product}"></Run>
            <Run Text="{x:Static Core:Publication.Version}"></Run>
        </TextBlock>
        <TextBlock Text="{Binding Components.Output.Description, Mode=OneWay, FallbackValue=FoxTunes}"></TextBlock>
    </StackPanel>
</UserControl>
