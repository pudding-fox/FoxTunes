<UserControl x:Class="FoxTunes.LibraryViewMode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Core="clr-namespace:FoxTunes;assembly=FoxTunes.Core" 
             xmlns:Windows="clr-namespace:FoxTunes" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance {x:Type Core:Core}, IsDesignTimeCreatable=False}" DataContextChanged="OnDataContextChanged">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Padding="0,0,7,0" ><Run Text="View Mode:"/></TextBlock>
        <ComboBox Grid.Column="1" ItemsSource="{Binding Components.Database.Sets.LibraryHierarchy}" SelectedValue="{Binding SelectedLibraryHierarchy, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Windows:LibraryViewMode}}}" DisplayMemberPath="Name"/>
        <Windows:LibraryHierarchies Grid.Column="2"></Windows:LibraryHierarchies>
    </Grid>
</UserControl>
