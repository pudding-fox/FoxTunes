<UserControl x:Class="FoxTunes.Playback"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Core="clr-namespace:FoxTunes;assembly=FoxTunes.Core" 
             xmlns:ViewModel="clr-namespace:FoxTunes.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Core:Core, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <ViewModel:Playback x:Key="ViewModel" Core="{Binding}"></ViewModel:Playback>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Width" Value="30"></Setter>
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="FontFamily" Value="Webdings"></Setter>
            <Setter Property="FontSize" Value="20"></Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding Source={StaticResource ViewModel}, Path=StopCommand}" CommandParameter="{Binding Managers.Playback.CurrentStream}">&lt;</Button>
        <Button Command="{Binding Source={StaticResource ViewModel}, Path=PlayCommand}" CommandParameter="{Binding Managers.Playback.CurrentStream}">4</Button>
        <Button Command="{Binding Source={StaticResource ViewModel}, Path=PauseCommand}" CommandParameter="{Binding Managers.Playback.CurrentStream}">;</Button>
        <Button>9</Button>
        <Button>:</Button>
    </StackPanel>
</UserControl>
